<UserControl x:Class="Intaller.Wpf.BusyButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="UserControl">
    <Button DataContext="{Binding ElementName=UserControl}" Command="{Binding Command}" Padding="0,0,20,0">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
            <Viewbox Margin="4" VerticalAlignment="Center">
                <mah:ContentControlEx VerticalAlignment="Center"
                                      HorizontalAlignment="Center" Content="{Binding ButtonContent}" Margin="4" />
            </Viewbox>
            <Image d:IsHidden="True" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverterInverted}}"
                   RenderOptions.BitmapScalingMode="HighQuality" Grid.Column="1" Source="{Binding Image}" />
            <Viewbox VerticalAlignment="Center" Grid.Column="1">
                <mah:ProgressRing IsActive="{Binding IsBusy}" />
            </Viewbox>
        </Grid>
    </Button>
</UserControl>